<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.fhict.org.hospitalmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="cs.fhict.org.hospitalceo.ui.employeeDetails.EmployeeDetailsActivity" />

        <activity android:name="cs.fhict.org.hospitalceo.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>